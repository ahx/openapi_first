#!/usr/bin/env ruby
# frozen_string_literal: true

`bundle update --all`
Dir.glob('Gemfile*')
   .reject { _1.end_with?('.lock') }
   .each do |filename|
  puts `BUNDLE_GEMFILE=#{filename} bundle update --all`
end
puts `cd benchmarks && bundle update --all && cd ..`
